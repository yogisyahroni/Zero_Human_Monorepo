ZH_CONFIG_PATH=./config/zero-human.yaml
REDIS_URL=redis://localhost:6379

# Generate with: openssl rand -hex 32
# PowerShell alternative:
# [System.Convert]::ToHexString([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32)).ToLower()
BETTER_AUTH_SECRET=

# Internal OpenAI-compatible token used by Hermes, Paperclip, and Codex when they call 9Router.
# Do not put a real provider key here.
ZH_ROUTER_COMPAT_API_KEY=

# AI provider keys are owned by 9Router. Prefer setting them in the 9Router UI
# at http://localhost:20128; these env values are only bootstrap/fallback for 9Router.
# Fill at least one provider key or configure it directly in the 9Router UI.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
GLM_API_KEY=

# Optional Codex executor model override. Keep combotest unless you intentionally
# want to bypass the 9Router combo routing configured in the 9Router UI.
CODEX_MODEL=combotest

DISCORD_WEBHOOK_URL=
